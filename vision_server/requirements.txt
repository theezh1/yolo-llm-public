# Vision server (FastVLM / Moondream). Heavy GPU deps — install on the GPU host.
# On RunPod PyTorch preset, torch is already present; the rest install fast.
torch>=2.2
transformers>=4.44
accelerate>=0.33
fastapi>=0.111
uvicorn>=0.30
pillow>=10.0
httpx>=0.27
# einops & timm are commonly required by small VLMs' trust_remote_code paths:
einops>=0.8
timm>=1.0
